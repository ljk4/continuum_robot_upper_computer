# 下位机代码实现指南

## 1. 项目概述

本项目是绳驱动并联连续体机器人的下位机固件，运行在 **STM32F407ZGTx** 上，通过 USART1 (PA9-TX / PA10-RX) 与上位机通信。
（初始调试阶段必须进行上位机断联举措，严格的限速，绳长限制和急停措施，否则可能损坏机器人）
### 系统架构

```
上位机 (PC, Python)
  │  39 字节定长帧协议 @ 115200 baud
  │
  ▼
STM32F407ZGTx
  ├── 通信层（已完成）
  │   ├── protocol.c    -- CRC16 / 帧打包 / 帧解析
  │   ├── usart.c       -- 串口驱动 + 环形缓冲区
  │   └── cmd_handler.c -- 帧组装 + 命令分发
  │
  ├── 控制层（待实现）
  │   └── control.c     -- PID 伺服 + 编码器 + 电机驱动
  │
  └── 主循环 (main.c)
      ├── cmd_handler_poll()       -- 解析上位机指令
      ├── control_update()         -- PID 计算 + 电机输出
      └── cmd_handler_send_feedback() -- 20Hz 编码器反馈
```

### 目录结构

```
2-5串口发送与中断接收实验/
├── CORE/               -- ARM Cortex-M4 核心头文件 + 启动文件
├── FWLIB/              -- STM32F4xx 标准外设库
│   ├── inc/            -- 库头文件
│   └── src/            -- 库源文件
├── SYSTEM/             -- 驱动层（通信 + 控制）
│   ├── usart.c/h       -- 串口驱动（已完成）
│   ├── protocol.c/h    -- 通信协议（已完成）
│   ├── cmd_handler.c/h -- 命令调度（已完成）
│   ├── control.c/h     -- 控制模块（待实现）
│   ├── delay.c/h       -- 延时函数
│   └── sys.c/h         -- 系统工具（GPIO 位带等）
├── USER/               -- 应用层
│   ├── main.c          -- 主程序入口
│   └── test.uvprojx    -- Keil 工程文件
└── WorkSpace/          -- 板级外设驱动
    ├── led.c/h         -- LED (PF9)
    └── key.c/h         -- 按键 (PE4, PA0)
```

---

## 2. 已完成的通信模块

以下模块已实现并测试通过，**无需修改**。

### 2.1 协议格式

```
[0xAA 0x55] [cmd 1B] [data 32B = 8×float32 LE] [CRC16 2B LE] [0x0D 0x0A]
 共 39 字节
```

| 方向 | 命令字 | 含义 | 数据区 |
|------|--------|------|--------|
| PC→STM32 | 0x01 | 发送目标值 | 8 个 float = 8 根绳的目标圈数 |
| PC→STM32 | 0x02 | 急停 | 全零 |
| PC→STM32 | 0x03 | 查询状态 | 全零 |
| STM32→PC | 0x81 | ACK 应答 | 全零 |
| STM32→PC | 0xA1 | 编码器反馈 | 8 个 float = 8 根绳的实际圈数 |

CRC16-Modbus：多项式 0xA001，初值 0xFFFF，对前 35 字节计算。

### 2.2 共享变量

`cmd_handler.c` 中定义了控制模块需要读写的全局变量：

```c
extern float   target_rotations[8];   // 上位机下发的目标圈数（控制模块读取）
extern float   encoder_rotations[8];  // 编码器实际圈数（控制模块写入）
extern uint8_t system_stopped;        // 急停标志
```

### 2.3 已实现的 API

```c
// protocol.c
uint16_t crc16_modbus(const uint8_t *data, uint16_t len);
void protocol_pack_ack(uint8_t *buf);
void protocol_pack_feedback(uint8_t *buf, const float *values);
uint8_t protocol_verify(const uint8_t *frame);

// cmd_handler.c
void cmd_handler_init(void);
void cmd_handler_poll(void);            // 每轮主循环调用
void cmd_handler_send_feedback(void);   // 20Hz 调用

// usart.c
void uart_init(u32 bound);
void uart_send_frame(const uint8_t *frame, uint16_t len);
```

---

## 3. 待实现：control.c 控制模块

### 3.1 文件位置

```
SYSTEM/control.c
SYSTEM/control.h
```

### 3.2 需要实现的函数

```c
void control_init(void);           // 初始化编码器 + 电机硬件
void control_update(void);         // PID 伺服（每轮主循环调用，约 250Hz）
void control_set_enable(uint8_t en); // 急停/使能（cmd_handler 已调用，无需修改）
```

### 3.3 control_init() 实现要点

```c
void control_init(void)
{
    // 步骤 1: 初始化编码器接口
    //   - 选择接口方式：定时器编码器模式 / I2C / SPI / ADC
    //   - 配置对应的 GPIO 和外设
    //   - 例：TIM2 编码器模式读取第 1 根绳的位置

    // 步骤 2: 初始化电机驱动接口
    //   - 舵机：配置 TIM 输出 PWM（50Hz，脉宽 0.5~2.5ms）
    //   - 直流电机：配置 TIM PWM + GPIO 方向控制

    // 步骤 3: 读取编码器初始位置
    //   - 将当前编码器值写入 encoder_rotations[0..7]
    //   - 同时将 target_rotations[0..7] 设为相同值（避免上电瞬间跳变）
}
```

### 3.4 control_update() 实现要点

```c
void control_update(void)
{
    if (!ctrl_enabled) return;  // 急停状态不动作

    for (i = 0; i < 8; i++) {

        // 步骤 1: 读取编码器实际位置
        encoder_rotations[i] = read_encoder(i);

        // 步骤 2: 计算误差
        error = target_rotations[i] - encoder_rotations[i];

        // 步骤 3: 死区判断
        if (fabs(error) < DEADBAND) continue;

        // 步骤 4: PID 计算
        output = Kp * error;  // 最简 P 控制

        // 步骤 5: 输出限幅
        output = CLAMP(output, -1.0, 1.0);

        // 步骤 6: 驱动电机
        set_motor(i, output);
    }
}
```

### 3.5 编码器读取示例（定时器编码器模式）

以 TIM2 读取第 1 根绳为例（需根据实际硬件接线修改）：

```c
// 初始化
void encoder_init(void)
{
    TIM_TimeBaseInitTypeDef  TIM_TimeBaseStructure;
    TIM_ICInitTypeDef        TIM_ICInitStructure;

    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2, ENABLE);

    // 配置 TIM2 为编码器模式
    TIM_EncoderInterfaceConfig(TIM2,
        TIM_EncoderMode_TI12,
        TIM_ICPolarity_Rising,
        TIM_ICPolarity_Rising);

    TIM_TimeBaseStructure.TIM_Period = 0xFFFF;  // 自动重装载值
    TIM_TimeBaseStructure.TIM_Prescaler = 0;
    TIM_TimeBaseInit(TIM2, &TIM_TimeBaseStructure);

    TIM_Cmd(TIM2, ENABLE);
}

// 读取
int16_t encoder_read(void)
{
    return (int16_t)TIM_GetCounter(TIM2);
}

// 转换为圈数
float encoder_to_rotations(int16_t counts)
{
    // 假设编码器线数 = ENCODER_LINES，减速比 = GEAR_RATIO
    // 一圈的计数值 = ENCODER_LINES * GEAR_RATIO * 4 (4倍频)
    return (float)counts / (ENCODER_LINES * GEAR_RATIO * 4.0f);
}
```

### 3.6 电机驱动示例（舵机 PWM）

```c
// 初始化 TIM3 CH1 输出 50Hz PWM（需根据实际引脚修改）
void servo_init(void)
{
    TIM_TimeBaseInitTypeDef  TIM_TimeBaseStructure;
    TIM_OCInitTypeDef        TIM_OCInitStructure;

    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM3, ENABLE);

    TIM_TimeBaseStructure.TIM_Period = 19999;      // 20000-1, 50Hz
    TIM_TimeBaseStructure.TIM_Prescaler = 83;       // 84MHz/(83+1)=1MHz
    TIM_TimeBaseStructure.TIM_ClockDivision = 0;
    TIM_TimeBaseStructure.TIM_CounterMode = TIM_CounterMode_Up;
    TIM_TimeBaseInit(TIM3, &TIM_TimeBaseStructure);

    TIM_OCInitStructure.TIM_OCMode = TIM_OCMode_PWM1;
    TIM_OCInitStructure.TIM_OutputState = TIM_OutputState_Enable;
    TIM_OCInitStructure.TIM_Pulse = 1500;  // 中位 1.5ms
    TIM_OCInitStructure.TIM_OCPolarity = TIM_OCPolarity_High;
    TIM_OC1Init(TIM3, &TIM_OCInitStructure);

    TIM_Cmd(TIM3, ENABLE);
}

// 设置舵机角度（归一化输入 -1.0 ~ +1.0）
void servo_set(uint8_t ch, float normalized)
{
    // 脉宽范围 500~2500us，中位 1500us
    uint16_t pulse = 1500 + (int16_t)(normalized * 1000);
    if (pulse < 500) pulse = 500;
    if (pulse > 2500) pulse = 2500;

    // 根据 ch 选择对应的 CCR 寄存器
    switch (ch) {
        case 0: TIM_SetCompare1(TIM3, pulse); break;
        case 1: TIM_SetCompare2(TIM3, pulse); break;
        // ... 其他通道
    }
}
```

---

## 4. main.c 主循环说明

当前主循环结构：

```c
int main(void)
{
    // 初始化
    NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);
    delay_init(168);            // SysTick, 168MHz
    uart_init(115200);          // USART1 串口
    LED_Init();
    KEY_Init();
    cmd_handler_init();         // 通信模块初始化
    control_init();             // 控制模块初始化（待实现）

    while (1)
    {
        cmd_handler_poll();         // 1. 解析串口收到的帧
        control_update();           // 2. PID 伺服计算 + 电机输出
        // 3. 20Hz 反馈（在 cmd_handler 内部通过计数实现）
        if (++loop_counter >= 50) {
            loop_counter = 0;
            cmd_handler_send_feedback();
        }
        delay_ms(1);               // 主循环周期约 4ms
    }
}
```

**执行顺序**：通信解析 → 控制计算 → 反馈发送 → 延时

---

## 5. 上位机通信协议详解

### 5.1 帧格式

| 偏移 | 长度 | 内容 | 说明 |
|------|------|------|------|
| 0-1 | 2B | 0xAA 0x55 | 帧头 |
| 2 | 1B | cmd | 命令字 |
| 3-34 | 32B | 8×float32 LE | 数据区 |
| 35-36 | 2B | CRC16 LE | 校验（对前 35 字节） |
| 37-38 | 2B | 0x0D 0x0A | 帧尾 |

### 5.2 目标值含义

`target_rotations[0..7]` 是 8 根绳从零位算起的**绝对圈数**（float，单位：圈）。
- 正值 = 绳收紧（舵机正转）
- 负值 = 绳放松（舵机反转）
- 上位机通过逆运动学计算后直接下发，下位机做位置伺服即可

### 5.3 反馈帧内容

`encoder_rotations[0..7]` 是 8 根绳的**当前实际圈数**。
- 上位机用此值计算跟踪误差
- 20Hz 发送频率（每 50ms 一帧）
- 下位机需保证此值反映真实编码器读数

### 5.4 急停行为

#### 上位机急停

收到 CMD_STOP (0x02) 后：
1. `control_set_enable(0)` 被调用
2. **不修改 target_rotations**，保持最后收到的目标值
3. PID 停止计算，电机保持当前位置（保持力矩，不要松力）
4. 回复 ACK

收到 CMD_TARGET (0x01) 后自动恢复 PID 控制，继续跟踪目标。

#### 本机硬件急停（独立于上位机）

| 按键 | 引脚 | 功能 | LED 指示 |
|------|------|------|----------|
| KEY0 | PE4（低电平有效） | 急停 | LED0 (PF9) 亮 |
| WK_UP | PA0（高电平有效） | 复位 | LED0 (PF9) 灭 |

工作流程：
1. **正常运行**：LED0 熄灭，PID 正常工作
2. **按下 KEY0**：立即停止 PID，电机保持当前位置，LED0 亮
3. **按下 WK_UP**：恢复 PID 控制，LED0 熄灭
4. 本机急停期间，上位机通信不受影响（仍可收发帧），但控制输出被禁止
5. 上位机发 CMD_STOP 也可触发急停，但本机只有按 WK_UP 才能复位

---

## 6. 调试流程

### 6.1 第一步：验证通信（已完成）

```bash
# 上位机运行
python test_comm.py
```

预期结果：
- CMD_QUERY → 收到反馈帧 (0xA1)，CRC 匹配
- CMD_TARGET → 收到 ACK (0x81)，CRC 匹配
- 3 秒内收到 ~60 帧 20Hz 反馈

### 6.2 第二步：验证控制模块编译

在 Keil 中编译，确认 0 Error 0 Warning。
将 `control_update()` 中的占位代码（`encoder = target`）替换为实际编码器读取后，烧录测试：

```bash
python test_comm.py
```

观察反馈帧中的编码器值是否反映真实位置。

### 6.3 第三步：单独测试编码器

临时在 `main.c` 中添加 printf 输出编码器值：

```c
// 临时调试代码（测试完成后删除）
printf("E:%.4f,%.4f,%.4f,%.4f,%.4f,%.4f,%.4f,%.4f\r\n",
       encoder_rotations[0], encoder_rotations[1],
       encoder_rotations[2], encoder_rotations[3],
       encoder_rotations[4], encoder_rotations[5],
       encoder_rotations[6], encoder_rotations[7]);
```

手动转动绳盘，观察编码器值变化是否正确。

### 6.4 第四步：单独测试电机

临时在 `main.c` 中添加固定输出测试电机：

```c
// 临时测试：让第 1 根绳的电机正转
target_rotations[0] = 1.0f;  // 目标 1 圈
```

观察电机是否正转，编码器值是否增加。然后测试负值（反转）。

### 6.5 第五步：闭环调试

1. 运行上位机：`python main.py`
2. 对准 AprilTag 标记
3. 观察上位机日志：
   - `[通信] 上次ACK间隔` 应 < 0.2s
   - `[误差] 最大` 应逐渐减小
   - `[目标] 范围` 应在合理范围内
4. 用串口助手监控反馈帧中的编码器值

### 6.6 第六步：PID 参数整定

在 `control.c` 中调整：

```c
#define PID_KP  0.01f   // 从小值开始，逐步增大
#define PID_KI  0.0f    // 先不用积分
#define PID_KD  0.0f    // 先不用微分
```

整定步骤：
1. Kp 从小开始（0.001），观察响应速度
2. 逐步增大 Kp 直到系统振荡，然后减小到振荡值的 60%
3. 如有稳态误差，加入少量 Ki
4. 如有超调，加入少量 Kd

---

## 7. 常见问题

### Q: 编译报错 `undefined symbol`

检查 `test.uvprojx` 中 SYSTEM 组是否包含所有 .c 文件：
- delay.c, sys.c, usart.c
- protocol.c, cmd_handler.c
- control.c

### Q: 上位机通信超时

1. 检查串口接线：PA9(TX)→RX, PA10(RX)→TX, GND→GND
2. 确认波特率 115200
3. 用串口助手手动发送 `AA 55 03 ...` 看是否有回复
4. 检查 CH340 驱动是否安装

### Q: 电机不动

1. 检查 PWM 输出引脚配置是否正确
2. 用示波器/逻辑分析仪测量 PWM 波形
3. 确认电机驱动板供电正常
4. 确认 `control_set_enable(1)` 被调用（发送 CMD_TARGET 后自动调用）

### Q: 编码器值不变化

1. 检查编码器接线
2. 确认编码器供电正常
3. 用 printf 输出原始计数值，手动转动轴观察
4. 检查定时器配置（编码器模式需 TI12 或 TI1）

### Q: 系统振荡

1. 减小 Kp
2. 增大死区 DEADBAND
3. 检查电机输出限幅 MAX_OUTPUT 是否合理
4. 检查编码器分辨率是否足够

---

## 8. 快速参考

### 关键文件速查

| 文件 | 作用 | 需要修改 |
|------|------|----------|
| `SYSTEM/control.c` | PID 控制 + 编码器 + 电机 | **是（主要工作）** |
| `SYSTEM/control.h` | 控制接口声明 | 可能（如需新增 API） |
| `USER/main.c` | 主循环 | 仅调试时临时加 printf |
| `SYSTEM/cmd_handler.c` | 命令分发 | 否 |
| `SYSTEM/protocol.c` | 协议解析 | 否 |
| `SYSTEM/usart.c` | 串口驱动 | 否 |

### 工程编译

1. Keil 打开 `USER/test.uvprojx`
2. Build (F7)
3. 确认 0 Error
4. Download (F8) 烧录

### 上位机测试命令

```bash
# 单独测试通信
python test_comm.py

# 运行完整系统
python main.py

# 查看日志
# 日志文件在 logs/ 目录下
```
